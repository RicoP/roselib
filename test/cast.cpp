#include <rose/cast.h>

#include <cassert>
#include <iostream>

/*
int main() {
  float f = 3.14f;
  int i = 42;
  double dd = 420000000000.7;
  size_t s = 314;

  assert($<int>(f) == 3);
  assert($<int>(dd / 10000000000.0) == i);
  assert($<int>(f) == 3);
  assert($<size_t>(f * 1000) == s * 10);

  return 0;
}

*/
