#pragma once

namespace rose {
    // Lokal handles are temporary ids that are only valid in the current context.
    typedef int local_handle;

    /*
    struct GUID {
        char data[16];
    };

    // Global handles are ids that are only valid beyond the application lifetime.
    typedef GUID global_handle;
    */
}